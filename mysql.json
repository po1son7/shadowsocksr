{
    "host": "${MYSQL_HOST}",
    "port": ${MYSQL_PORT},
    "user": "${MYSQL_USER}",
    "password": "${MYSQL_PASS}",
    "db": "${MYSQL_DB}",
    "node_id": 0,
    "transfer_mul": ${TRANSFER},
    "ssl_enable": 0,
    "ssl_ca": "",
    "ssl_cert": "",
    "ssl_key": ""
}